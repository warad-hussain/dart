void main() {
  // Operators
  /* if the conditions is complex so its better to use parentheses
  // Parentheses improve readability.
  if ((n % i == 0) && (d % i == 0)) ...

  // Harder to read, but equivalent.
  if (n % i == 0 && d % i == 0) ...*/

/*  // Types of operators in dart
  // to read all about operators visit here https://dart.dev/language/operators

   //These operators are widely used :
//  1.  Arithmetics operators +, -, *, /
//  2.  Relational Operators < , > , == , <= , >= , !=
//  3.  Logical Operators  && , || , !
*/


// if,else if and else statement

/*stdout.write("Enter your age :");
int age = int.parse(stdin.readLineSync()!);

if(age>=18){
  print('you are adult!');
}else if(age<12){
  print("You are a baby child!");
}else{
  print("You are  child!");
}
*/

/*// I knows about them so lets explore ternary operator
stdout.write("Enter your name :");
String name = stdin.readLineSync()!;
String ename = name.startsWith("Warad") ? "Welcome! Warad Hussain." : "Chala ja yaha se!";
// so question mark tells if the condtion met then print this and the semicolon : tell if the condition doesnt met then print it
print(ename);
*/


/*// Switch Case Statement

  stdout.write("Enter your Age :");
  int age = int.parse(stdin.readLineSync()!);

  switch (age) {
    case 21 when age >20: // by adding when it means both conditions should be true either it will go on next
      print("Hi");
    default:
      print("Bye");
  }
 */







}